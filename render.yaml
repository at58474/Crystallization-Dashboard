services:
  - type: web
    name: crystallization-dashboard
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 2 -b 0.0.0.0:10000 main:server"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
